# Manuscript PDF

`axial_matter_channel_ell1_preprint.pdf` is the current compiled REVTeX author
manuscript. Rebuild it, together with all figures, using:

```powershell
python paper/build_preprint.py
```

Submission archives are generated separately by
`paper/package_prd_audit_repair.py` and are not treated as source artifacts.
