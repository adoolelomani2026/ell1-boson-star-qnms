# Symbolic checks

This directory stores compact, human-readable SymPy reductions used to verify
the axial Klein--Gordon coupling and directly linearized metric operator.

Regenerate them with:

```powershell
python -m experiments.derive_axial_kg_coupling
python -m experiments.derive_axial_metric_operator
```
